services:
  registry:
    build:
      context: ..
      dockerfile: crates/relayorb-registry/Dockerfile
    environment:
      RELAYORB_ENV: dev
      RELAYORB_REGION: local
      RELAYORB_VERSION: dev
      RELAYORB_SERVICE_NAME: relayorb-registry-dev
      REGISTRY_BIND_ADDR: 0.0.0.0:8081
      DATABASE_URL: sqlite:///data/registry.db?mode=rwc
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      RELAYORB_METRICS_EXPORTER: prometheus
      METRICS_AUTH_MODE: public
    ports:
      - "8081:8081"
    volumes:
      - registry-data:/data
    depends_on:
      - otel-collector

  gateway:
    build:
      context: ..
      dockerfile: crates/relayorb-gateway/Dockerfile
    environment:
      RELAYORB_ENV: dev
      RELAYORB_REGION: local
      RELAYORB_VERSION: dev
      RELAYORB_SERVICE_NAME: relayorb-gateway-dev
      GATEWAY_BIND_ADDR: 0.0.0.0:8080
      REGISTRY_URL: http://registry:8081
      DATABASE_URL: sqlite:///data/gateway.db?mode=rwc
      AUTH_MODE: hmac
      SECRET_AUTH_HMAC: relayorb-dev-secret
      POLICY_PATH: config/policy.toml
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      RELAYORB_METRICS_EXPORTER: prometheus
      METRICS_AUTH_MODE: public
    ports:
      - "8080:8080"
    volumes:
      - gateway-data:/data
    depends_on:
      - registry
      - otel-collector

  worker:
    build:
      context: ..
      dockerfile: crates/relayorb-samples/worker-mock-rag/Dockerfile
    environment:
      RELAYORB_ENV: dev
      RELAYORB_VERSION: dev
      RELAYORB_SERVICE_NAME: relayorb-rag-dev
      REGISTRY_URL: http://registry:8081
      WORKER_BIND_ADDR: 0.0.0.0:8090
      WORKER_BASE_URL: http://worker:8090
      RELAYORB_PUBLIC_BASE_URL: http://worker:8090
      WORKER_INSTANCE_ID: worker-mock-rag-1
      RELAYORB_REGION: us-central1
      RAG_LIVE_SEARCH: ${RAG_LIVE_SEARCH:-0}
      RAG_LIVE_BACKEND: ${RAG_LIVE_BACKEND:-wikipedia}
      RAG_WIKI_BASE_URL: ${RAG_WIKI_BASE_URL:-https://en.wikipedia.org}
      RAG_HTTP_USER_AGENT: ${RAG_HTTP_USER_AGENT:-relayorb-worker-mock-rag/0.1}
      RAG_PROVIDER_NAME: ${RAG_PROVIDER_NAME:-}
      RAG_RESPONSE_DELAY_MS: ${RAG_RESPONSE_DELAY_MS:-0}
      WORKER_TTL_SECONDS: ${WORKER_TTL_SECONDS:-60}
      WORKER_HEARTBEAT_INTERVAL_SECONDS: ${WORKER_HEARTBEAT_INTERVAL_SECONDS:-20}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      RELAYORB_METRICS_EXPORTER: prometheus
      METRICS_AUTH_MODE: public
    ports:
      - "8090:8090"
    depends_on:
      - registry

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: relayorb
      POSTGRES_PASSWORD: relayorb
      POSTGRES_DB: relayorb
    volumes:
      - pg-data:/var/lib/postgresql/data

  otel-collector:
    image: otel/opentelemetry-collector:0.109.0
    command: ["--config=/etc/otelcol/config.yaml"]
    volumes:
      - ./otel-collector-config.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "4317:4317"
      - "4318:4318"

volumes:
  registry-data:
  gateway-data:
  pg-data:
