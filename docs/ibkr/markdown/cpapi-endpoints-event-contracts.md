# CPAPI Endpoints – Event Contracts

### Event Contracts

### Category and market tree

Provides event category and market tree

#### Request Object:

  - Python
  - cURL

<!-- end list -->

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/category/tree"
requests.get(url=request_url)
```

#### Request Object:

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

### Category markets

Provides markets from category tabs in markets UI

#### Request Object:

###### Body Content

  - Python
  - cURL

<!-- end list -->

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/market/category"
requests.post(url=request_url)
```

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

``` EnlighterJSRAW
{}
```

### Underlying market contracts

Provides all contracts for given underlying market

#### Request Object:

###### Query Parameters

**underlyingConid:** int. Required  
Underlying event index contract identifier.

**exchange:** String.  
Exchange to filter contracts by.

  - Python
  - cURL

<!-- end list -->

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

  - Python
  - cURL

<!-- end list -->

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

  - Python
  - cURL

<!-- end list -->

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

  - Python
  - cURL

<!-- end list -->

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```

#### Request Object:

``` EnlighterJSRAW
request_url = f"{baseUrl}/forcast/trader/"
requests.get(url=request_url)
```
