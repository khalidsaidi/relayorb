# Conformance Assets

- `manifests/`: capability conformance manifest definitions.
- `vectors/`: runtime case vectors for each capability.
- `reports/`: machine-readable run output from `relayorb-conformance run` (gitignored).

Baseline capability under test:
- `rag.search@v1`
